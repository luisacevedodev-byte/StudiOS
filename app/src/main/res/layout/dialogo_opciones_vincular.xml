<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="?attr/colorSurface">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp">

        <TextView
            android:id="@+id/tvTituloVincular"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Vincular Cuenta"
            android:textColor="@color/studios_cyan_titulo"
            android:textSize="22sp"
            android:textStyle="bold"
            android:shadowColor="@color/studios_cyan_sombra"
            android:shadowRadius="15"/>

        <ImageButton
            android:id="@+id/btnCerrarDialogo"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_alignParentEnd="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_close"
            app:tint="@color/gris_texto" />
    </RelativeLayout>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Conecta tu cuenta para respaldar tus datos en la nube."
        android:textColor="@color/gris_texto"
        android:textSize="14sp"
        android:layout_marginBottom="24dp"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnGoogle"
        style="@style/Widget.MaterialComponents.Button.OutlinedButton"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="Continuar con Google"
        android:textColor="?attr/colorOnSurface"
        android:textAllCaps="false"
        android:textStyle="bold"
        app:strokeColor="@color/gris_texto"
        app:cornerRadius="12dp"
        app:icon="@drawable/ic_google"
        app:iconGravity="textStart"
        android:layout_marginBottom="12dp"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnCorreo"
        style="@style/Widget.MaterialComponents.Button.OutlinedButton"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:text="Continuar con Correo"
        android:textColor="?attr/colorOnSurface"
        android:textAllCaps="false"
        android:textStyle="bold"
        app:strokeColor="@color/gris_texto"
        app:cornerRadius="12dp"
        app:icon="@drawable/ic_email"
        app:iconGravity="textStart"/>
</LinearLayout>